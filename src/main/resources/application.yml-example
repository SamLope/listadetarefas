spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lista_tarefas
    username: postgres
    password: SUA_SENHA_DO_POSTGRES_AQUI
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080